import { Highlight2 } from './highlight2';

describe('Highlight2', () => {
  it('should create an instance', () => {
    const directive = new Highlight2();
    expect(directive).toBeTruthy();
  });
});
